<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.dao.UserMapper">
    <resultMap id="UserResultMap" type="com.library.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="age" column="age"/>
        <result property="sex" column="sex"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectByUsername" resultMap="UserResultMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <insert id="insertUser" parameterType="com.library.entity.User">
        INSERT INTO user (username, password, age, sex, create_time, update_time)
        VALUES (#{username}, #{password}, #{age}, #{sex}, #{createTime}, #{updateTime})
    </insert>

    <select id="login" resultMap="UserResultMap">
        SELECT * FROM user WHERE username = #{username} AND password = #{password}
    </select>

    <select id="checkUsername" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM user WHERE username = #{username}
    </select>
</mapper>
